#include "main.h"
/**
* _abs- prints the absolute value of the integer
* @n: The integer to be computed
* Return: 0 Always, otherwise non-zero
*/
int _abs(int n)
{
if (n >= 0)
{
return (n);
}
else
{
return (-n);
}
}
